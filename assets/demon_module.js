  /* ===================== 악마 패턴 - 현재 꺼져 있음 =====================
     2026-09-23 밤: 실제 플레이에서 장애물 간격이 너무 좁아 도저히 넘을 수 없다는
     보고를 받고, 패턴 전체를 무동작으로 되돌렸다.

     이 파일은 지금 아무 일도 하지 않는다. 아래 두 함수는 assets/endless_boot.js 가
     game.js 안에 끼워넣은 호출 지점(demonReset / demonUpdate)을 채우기 위한
     껍데기일 뿐이다. 따라서:

       - 장애물 스폰 로직에 전혀 손대지 않는다. 예전부터 있던 패턴(똥+개 콤보,
         쥐 콤보, 까마귀 경고 등)은 모두 원래 그대로 동작한다.
       - crowPending / spawnTimer / nextSpawnIn 을 건드리지 않는다.
       - obstacles 배열에 아무것도 넣지 않는다.

     다시 만들 때 주의할 점 (이전 실패 원인 기록):
       간격을 "화면 높이(appH)의 배수"로 잡고 시뮬레이션으로만 검증했는데, 실제
       화면에서 눈으로 확인하지 않았다. 숫자상으로는 1단 점프 이동거리(0.750 appH)
       보다 넓었지만 실제 플레이 감각과 맞지 않았다. 다음 설계에서는 반드시
       스크린샷으로 배치를 눈으로 확인하고, 사람이 한 판 해본 뒤에 올린다.

     이전 버전은 GitHub 이력에 남아 있다 (assets/demon_module.js 커밋 f7d8c654).
     ==================================================================== */
  function demonReset(){ /* 아무것도 하지 않는다 */ }
  function demonUpdate(dt){ /* 아무것도 하지 않는다 */ }
